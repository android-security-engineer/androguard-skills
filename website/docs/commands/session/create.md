# session create

> Create a new Session (held for subsequent session commands; use daemon mode for cross-command persistence)

## 用法

```bash
androguard-skills session create
```

## 参数

无业务参数。

## 说明

Create a new Session (held for subsequent session commands; use daemon mode for cross-command persistence)

## 对应 API

`skills.session_create(...)` — 详见 [Python API](../../guide/python-api)。

## 相关

- [session 命令组](./)
- [命令索引](../)
