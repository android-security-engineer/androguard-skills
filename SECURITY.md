# 安全政策（Security Policy）

本仓库是面向 **Android 安全分析与逆向工程** 的工具，处理的是潜在恶意的 APK / DEX / 样本文件。安全是我们的核心关切。

## 支持的版本

安全修复会以补丁形式发布到最新的稳定版本。推荐始终升级到最新 Release。

| 版本 | 是否支持 |
|------|---------|
| 最新 Release（含 v4.2.0） | ✅ 支持 |
| 旧版本 | ⚠️ 仅在可合理修复时提供 |

## 报告漏洞（Reporting a Vulnerability）

请**不要**通过公开的 Issue 报告安全漏洞（包括：远程执行、反序列化、路径遍历、资源耗尽/DoS、样本文件导致的崩溃等）。

请通过以下私密渠道报告：

- **首选**：向仓库维护者发送私密消息 / 邮件，或使用 GitHub 的 [Private vulnerability reporting](https://docs.github.com/en/code-security/security-advisories)（若已启用）。
- **备选**：直接联系 `CC11001100`（GitHub 账号，见仓库提交者）。

请在报告中提供：
1. 漏洞类型与影响。
2. 触发所需的最小样本或复现步骤（恶意 APK/DEX 样本建议先压缩/脱敏）。
3. 受影响的版本与模块。

**承诺**：我们会在收到后 7 天内确认，并尽快提供修复。

## 使用安全提示

- 本工具会**解析不可信的 Android 样本**。历史上 DEX/APK 解析器曾有过资源耗尽与拒绝服务类问题。在生产或 CI 中处理样本时，建议在沙箱/受限环境中运行，并限制单次解析的资源占用。
- 启用 daemon 模式时，默认监听 `127.0.0.1`，请勿将其暴露到不可信网络；MCP stdio server 仅在本地进程间使用。

## 依赖安全

我们依赖上游第三方库（lxml、cryptography、networkx 等）。若你发现依赖存在 CVE，也请通过上述渠道告知。
