# apk security-overview

> Security audit overview (signatures/permissions/components/manifest flags/packing)

## 用法

```bash
androguard-skills apk security-overview
```

## 参数

无业务参数。

::: tip 公共参数 `--apk-path`

支持 `--apk-path <路径>` 或环境变量 `ANDROGUARD_APK_PATH`。daemon 模式下若已 `load` 可省略。

:::

## 说明

Security audit overview (signatures/permissions/components/manifest flags/packing)

## 对应 API

`skills.apk_security_overview(...)` — 详见 [Python API](../../guide/python-api)。

## 相关

- [apk 命令组](./)
- [命令索引](../)
